[project]
name = "ragify-python"
version = "0.1.0"
description = "Python & FastAPI based backend for ragify"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.19.1",
    "cors>=1.0.1",
    "fastapi[standard]>=0.141.1",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
    "sqlalchemy>=2.0.52",
]
